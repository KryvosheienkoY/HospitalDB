<nav class="navbar navbar-expand navbar-dark bg-dark">
    <a class="navbar-brand" style="color:#ffcc04">Система обліку пацієнтів </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    </button>
    <ul class="nav navbar-nav">
        <li class="nav-item <%= section === 'admin' ? 'active' : '' %>">
            <a class="nav-link" href="/admin">| ADMIN</a>
        </li>

    </ul>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="nav navbar-nav ml-auto">
            <li class="nav-item" id="loginAid">
                <a class="nav-link trigger-btn" href="/" data-toggle="modal"
                   data-target="#modalLoginForm" id ="logoutIdA">Logout</a>
            </li>
        </ul>
    </div>
    <script>
        $('#logoutIdA').on('click', function (e) {
            e.preventDefault();
            //  console.log("token -" + sessionStorage.getItem('token'));
            $.ajax({
                type: 'GET',
                // headers: {Authorization: sessionStorage.getItem("token")},
                url: 'http://localhost:8080/',
                success: function (response) {
                    console.log("success of //");
                    console.log(response);
                    $("body").html(response);
                }
            });
        });
    </script>
</nav>
